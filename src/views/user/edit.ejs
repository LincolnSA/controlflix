<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Controle | Netflix</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>

    <header>

        <nav class="navbar navbar-expand-lg  navbar-dark bg-dark">
            <div class="container-fluid container">
                <a class="navbar-brand" href="/">Voltar</a>
            </div>
        </nav>

    </header>

    <main>

        <div class="container">
            <form action="/usuarios/<%= user.id %>/editar" method="POST" class="mt-4">

                <h5>Editar usuário</h5>
                <div class="mb-3">
                    <label for="name" class="form-label">Nome</label>
                    <input required type="text" class="form-control" id="name" name="name" value="<%= user.name %>">
                </div>
                <div class="mb-3">
                    <label for="devices" class="form-label">Dispositivos</label>
                    <textarea required class="form-control" id="devices" rows="2"
                        name="devices"><%= user.devices %></textarea>
                </div>
                <div class="mb-3">
                    <label for="value" class="form-label">Valor</label>
                    <input required type="text" class="form-control" id="value" name="value" value="<%= user.value %>">
                </div>
                <div class="mt-4 d-grid">
                    <button type="submit" class="btn btn-success">Salvar</button>
                </div>

            </form>
        </div>

    </main>

</body>

</html>